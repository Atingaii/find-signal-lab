# Data Layout

- `data/raw/`: untouched source snapshots
- `data/interim/`: normalized extracts and temporary outputs
- `data/processed/`: SQLite database and processed datasets

The first planned warehouse file is `data/processed/fund_predictor.db`.
